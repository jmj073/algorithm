#include <iostream>

#define loop(i, b, e) for (decltype(b) i = (b), __e = (e); i < __e; ++i)

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    
}